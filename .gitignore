# ===========================================================================
#  2021_dAu200  —  .gitignore
#
#  策略 / Policy
#    * 二进制文件：全部忽略（按扩展名列出，见下）。
#    * 文本文件：全部保留（.cxx/.h/.C/.txt/.md/.json/Makefile/脚本 …）。
#
#  注意 / Note
#    这是「黑名单」策略：新增一种二进制格式时，需要在下面对应分区补一条规则。
#    末尾「文本永远保留」一段是显式白名单，用来保证任何将来新增的二进制规则
#    都不会误伤已有的源码/配置类型。
#
#  历史 / History
#    旧版本内容是 `roots/` + `*.root`。其中 `roots/` 会把
#    `roots/Compare_dAu_OO/compare.C`、`compare2.C` 两个源码文件一起忽略掉，
#    与「保留所有文本文件」的目标冲突，故改为按扩展名忽略二进制。
# ===========================================================================

# ---- ROOT / 分析产物 -------------------------------------------------------
*.root
*.root.old
*.pcm
*.pch

# ---- 图片 ------------------------------------------------------------------
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.tif
*.tiff
*.webp
*.ico
*.pdf

# ---- 压缩包 / Office 文档 --------------------------------------------------
*.zip
*.tar
*.tar.gz
*.tgz
*.gz
*.bz2
*.xz
*.7z
*.rar
*.doc
*.docx
*.xls
*.xlsx
*.ppt
*.pptx

# ---- 编译产物 / 共享库 / 可执行文件 ----------------------------------------
*.o
*.a
*.obj
*.lib
*.so
*.dylib
*.dll
*.exe
*.mod

# ---- 其它二进制数据格式 ----------------------------------------------------
*.pyc
*.pyd
*.npy
*.npz
*.bin
*.h5
*.hdf5
*.parquet
*.db
*.sqlite
*.sqlite3

# ---- 操作系统 / 编辑器垃圾文件 ---------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
*~

# ---- 文本永远保留（显式白名单，防止上面的规则误伤） ------------------------
!*.cxx
!*.cpp
!*.cc
!*.h
!*.hpp
!*.C
!*.txt
!*.md
!*.json
!*.yml
!*.yaml
!*.xml
!*.csv
!*.csh
!*.sh
!*.py
!*.consign
!Makefile
!*.mk
!README*
!.gitignore
!.gitattributes
